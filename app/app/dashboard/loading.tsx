import { SplashView } from "@/components/splash-view";

/** Man cho khi dang tai /dashboard/* (server kiem tra phien + doc D1) - user
 * chot 09-24b: Splash chi hien trong luc dang tai that. */
export default function DashboardLoading() {
  return <SplashView />;
}
