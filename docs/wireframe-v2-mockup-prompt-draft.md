# Prompt — vẽ wireframe THẬT (mockup trực quan) từ spec đã chốt

Dán vào Claude Chat (dùng cửa sổ đã vẽ Group A-E càng tốt, đã có context; nếu không thì dán kèm nội dung `docs/04-wireframe-v2.md`):

```
Đóng vai Senior Product Designer. Mình đã có spec wireframe bằng chữ (hệ lưới 10 hàng, mô tả từng hàng cho 5 nhóm màn: Home, Màn quét QR, Options dropdown, Popup Tip Setting, Popup Lịch sử giao dịch) — giờ cần bạn VẼ THẬT ra thành mockup trực quan (không phải mô tả bằng chữ nữa), dùng Artifact (HTML/CSS), đúng tỷ lệ khung 375×812, đúng hệ lưới 10 hàng đã mô tả.

Vẽ từng nhóm màn một, dùng đúng khung/tỷ lệ hàng đã chốt trong spec — không tự đổi bố cục, chỉ cần biến mô tả chữ thành hình. Màu sắc/font dùng tạm placeholder đơn giản (đen trắng/xám là đủ), CHƯA cần đúng màu thương hiệu thật — bước phối màu/font làm riêng sau.

Vẽ xong mỗi nhóm, dừng lại chờ mình xác nhận đúng ý trước khi sang nhóm tiếp theo.

Đây là spec wireframe đã chốt:
```

*(dán tiếp nội dung `docs/04-wireframe-v2.md` ngay sau đây)*

---

Xong thì gửi link Artifact (hoặc ảnh chụp) về đây để đối chiếu trước khi mình code UI Home thật theo đúng mockup.
